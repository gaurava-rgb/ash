select id, to_char(changed_at, 'YYYY-MM-DD HH24:MI') as "when", table_name || ' ' || row_code as what, field, old_value, new_value, changed_by
from audit_log where (table_name = 'requests' and row_id = {{appsmith.URL.queryParams.id}}) or (table_name = 'samples' and row_id in (select id from samples where request_id = {{appsmith.URL.queryParams.id}})) order by changed_at desc, id desc limit 20;